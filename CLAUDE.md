# ScratchPad（家族总规则见上级 CLAUDE.md）

阅后即焚手写白板：iPad+Pencil 为主，Zoom 通话时 PC 也用。UI 中文。

- 数据：**纯本地，无云 by design**——不要给它加云同步。
- 手感细节是人类调过的：压感默认关（开=全压感）、双击橡皮、两指/三指 undo redo（抄 WebPaint）、LaTeX/文本框（Word 式，回车渲染，html2canvas 导出）。
- stylus 事件与 resize 漂移的坑直接读 WebPaint 的文档和源码，不要重新踩。
